1. **页面标题**：通过 API 动态设置 `<title>`，示例接口 `GET /api/site-title`，返回 `{"title": "南宁KTV"}`，在 JS 调用处注释清楚。
2. **南宁各区 KTV 列表**：南宁共 7 个区（青秀区、兴宁区、西乡塘区、良庆区、江南区、邕宁区、武鸣区），每个区的 KTV 数据从 API 获取，示例接口 `GET /api/ktv?district=青秀区`，需返回该区 KTV 的数组。循环渲染每个区的 KTV 信息，JS 调用处必须有详细注释。
3. **文章列表**：按发布类型获取文章，示例接口 `GET /api/articles?type=news`，调用处注释清楚。
4. **注释规范**：所有 API 调用的 JS 代码上方，用多行注释写明接口地址、请求方法、参数示例和返回数据示例，方便后端对照。
5. **SEO 基础**：使用语义化标签（`<header>`, `<nav>`, `<main>`, `<section>`, `<footer>`），添加 `meta description` 和 `meta keywords`，内容优先写在 HTML 中，JS 只用于补充动态数据。