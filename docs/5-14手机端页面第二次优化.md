# 5/14 手机端页面第二次优化

> 日期：2026-05-14  
> 范围：手机端 + PC端 + SEO

---

## 一、手机端优化

### 1. H1 副标题 p 标签字号统一

| 位置 | 修改前 | 修改后 |
|------|--------|--------|
| 768px `.section-ktv-home .section-title p` | 0.5rem | **0.9rem**（与热门KTV门店 p 一致） |
| 480px `.section-ktv-home .section-title p` | 0.45rem | **0.65rem**（与热门KTV门店 p 一致） |

### 2. 首页 7 区卡片加高 + 适配

| 项目 | 修改前 | 修改后 |
|------|--------|--------|
| `.district-home-card` min-height（768px） | 100px | **130px** |

### 3. 详情页/城区页面包屑删除 + 标题居中

| 文件 | 修改 |
|------|------|
| `detail.html` | 删除 `.detail-breadcrumb` 面包屑导航 |
| `district.html` | 删除 `.breadcrumb` 面包屑，删除 CSS 样式 |
| `district.html` | `#pageH1` 和 `#pageSubtitle` 居中处理 |

### 4. 全站电话号码统一为 19968122123

| 文件 | 修改 |
|------|------|
| `district.html` 弹窗默认电话 | `138xxxx8888` → `19968122123` |
| `district.html` JS 模拟数据 phone | `138xxxx` + 序号 → `19968122123` |
| `js/detail.js` 模拟数据 phone（详情+推荐） | `138xxxx` + 序号 → `19968122123` |

### 5. 首页地区导航栏（仅手机端）

在 `<header>` 下方新增 `.index-district-nav` 导航栏，包含7个区标签：

- **全部** / 青秀区 / 兴宁区 / 西乡塘区 / 良庆区 / 江南区 / 邕宁区 / 武鸣区
- 样式：药丸标签，横向可滚动，sticky 定位
- 点击任意标签 → 平滑滚动到 `#featuredStores`（热门KTV门店 h2）
- 默认高亮"全部"
- PC端 `display: none` 隐藏

**CSS 修改**：
- `css/style.css` 新增 `.index-district-nav` 默认隐藏 + 768px 媒体查询显示
- `#featuredStores` scroll-margin-top 从 70px → **110px**（预留地区导航栏高度）

**JS 修改**：
- `js/main.js` 新增地区导航栏点击事件处理

### 6. float-wechat 跨页面持久化

| 文件 | 修改 |
|------|------|
| `district.html` | 新增 `.float-wechat` 元素 + 点击切换 JS |
| `articles.html` | 新增 `.float-wechat` 元素 |
| `article-detail.html` | 新增 `.float-wechat` 元素 |
| `js/articles.js` | 新增 float-wechat 点击切换事件 |
| `js/article-detail.js` | 新增 float-wechat 点击切换事件 |
| `css/style.css` 768px | `.float-wechat` 添加 `display:flex !important; opacity:1 !important; visibility:visible !important` |

现在所有8个页面均包含 `.float-wechat` 元素，且 CSS 确保始终可见。

---

## 二、SEO 优化

| 文件 | 优化项 |
|------|--------|
| `district.html` | 新增 WebPage JSON-LD 结构化数据 |
| `article-detail.html` | 补充 `og:image:width/height` |

---

## 三、文件修改清单

| 文件 | 修改类型 |
|------|----------|
| `css/style.css` | p 字号 + 卡片高度 + 地区导航栏样式 + pageHeader居中 + float-wechat可见 + scroll-margin |
| `index.html` | 新增 `.index-district-nav` 地区导航栏 |
| `district.html` | 删除面包屑 + 居中标题 + 电话19968122123 + 添加float-wechat + WebPage JSON-LD |
| `detail.html` | 删除 detail-breadcrumb |
| `articles.html` | 添加 float-wechat |
| `article-detail.html` | 添加 float-wechat + og:image 尺寸 |
| `js/main.js` | 地区导航栏点击事件 |
| `js/detail.js` | 电话19968122123 + 删除面包屑JS |
| `js/articles.js` | float-wechat 点击切换 |
| `js/article-detail.js` | float-wechat 点击切换 |
